Place your images here with the exact filenames used by the landing page:

- body.png      (default/idle cat at the bottom)
- body_2.png    (midair cat during jumps)
- happy.png     (final happy cat state)

Recommended sizes: similar aspect ratios across frames for smooth swaps. The code scales the <img> to min(38vw, 320px).
